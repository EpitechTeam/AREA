var mongoose     = require('mongoose');
var Schema       = mongoose.Schema;

var InstagramSchema   = mongoose.Schema({
});

var Instagram = module.exports = mongoose.model('Instagram', InstagramSchema);
